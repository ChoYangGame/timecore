# TimeCore — 프로젝트 규칙

NHN 게임 AI 해커톤 예선 출품작. 2인 팀(개발 1, 디자인 1). 마감 임박.

## 기술 제약 (위반 시 빌드가 깨짐)

- 타깃은 **Unity 6 / Web(WebGL)** 빌드다. WebGL이 지원하지 않는 API를 쓰지 말 것:
  - 멀티스레딩 (`Thread`, `Task.Run`, `Parallel`)
  - `System.IO` 파일 접근 → 저장은 `PlayerPrefs`만
  - 소켓, `System.Net` 기반 통신
- 빌드 용량 = 로딩 시간이다. 새 패키지나 에셋 도입 전에 반드시 먼저 물어볼 것.
- 목표 사양은 저사양 노트북 브라우저다. 최적화보다 안정적 프레임이 우선.
- URP 2D Light는 적이 많아지면 비싸다. 분위기는 스프라이트 색조로 처리.

## Unity 컴파일 처리

- Auto Refresh는 켜져 있다. Ctrl+R 요청은 효과가 없으니 하지 마라.
- DLL 타임스탬프 폴링 절대 금지.
- 컴파일 결과는 Unity_GetConsoleLogs로만 확인한다.
- 스크립트가 인식되지 않으면 사용자에게 "Unity 종료 → Library/ScriptAssemblies 삭제 → 재시작"을 요청한다.
- Unity_RunCommand의 빌드 결과가 "failed"여도 Unity_GetConsoleLogs의 에러가 0건이면,
  Build/ 폴더 타임스탬프와 용량으로 실제 성공 여부를 판단한다.
  Sentis 패키지(com.unity.ai.inference)의 셰이더 경고는 무시한다.

## 폰트 서브셋 갱신

- TMP 폰트 굽기는 MCP로 불가능하다 (인터랙티브 다이얼로그가 차단됨).
- 새 글자가 필요하면 코드로 시도하지 말고, 아래를 사용자에게 전달하고 대기한다:
  (1) 기존 포함 글자 + 새 글자를 합친 한 줄 문자열
  (2) 설정값 (Padding 5px / Atlas 1024 / SDFAA)
- 사용자가 Font Asset Creator로 직접 굽는다.
- **새 애셋으로 만들면 안 된다.** Unity 6에서 Font Asset Creator로 새로 만들면
  `TMP_FontAsset`이 아니라 `UnityEngine.TextCore.Text.FontAsset` 타입으로 생성돼
  씬의 모든 TMP 텍스트가 폰트 참조를 잃는다 (2026-08-02 실제 발생).
  반드시 기존 `Assets/Fonts/Pretendard SDF` 인스펙터의 **Update Atlas Texture**로 열어
  제자리 갱신하고 **Save**(Save as 아님)로 덮어쓴다.
- 폰트 참조가 깨졌을 때: 파일을 git으로 되돌린 뒤
  `AssetDatabase.ImportAsset(ForceUpdate)` + 씬 재로드로 복구된다.
  Play 모드 종료나 Library 삭제로는 안 풀린다.

## 작업 방식

- 확장성보다 지금 돌아가는 것이 우선. 완벽한 아키텍처를 제안하지 말 것.
- 먼저 요청하지 않는 한 리팩터링하지 말 것.
- 새 도구나 워크플로우를 도입하지 말 것.
- 코드는 클래스 단위로 완결되게, 어느 GameObject에 붙이는지 명시할 것.

## 코드 리뷰

- 리뷰 요청 시 docs/REVIEW_LOG.md를 먼저 읽고,
  이미 기각·보류된 항목을 다시 제안하지 않는다.
- 리뷰 원문은 docs/reviews/YYYY-MM-DD-N회차.md에 **요약 없이 전문** 저장한다.
- 판단과 추적은 docs/REVIEW_LOG.md에서 관리한다.
- 지적 해결 시 "미해결"에서 "해결 완료"로 옮기고 커밋 해시를 남긴다.
- 리팩터링·아키텍처 개선 제안은 하지 않는다.

## 필수: 작업 세션 종료 시 로그 기록

의미 있는 작업(기능 구현, 버그 해결, 설계 결정)을 끝낼 때마다
docs/AI_LOG.md 맨 아래에 append 한다. 기존 내용은 절대 수정·삭제하지 않는다.

형식:

```
### YYYY-MM-DD (D-N) | 한 줄 요약

- **도구**: Claude Code (모델명) / Unity MCP / 기타
- **작업**: 무엇을 만들었거나 고쳤는지
- **프롬프트 원문**: 결정적이었던 지시를 그대로 인용. 요약 금지. 없으면 "해당 없음"
- **설계 판단과 근거**: 왜 그 방식을 택했는지, 어떤 대안을 버렸는지
- **검증 방법**: 추정이 아니라 무엇을 실측했는지 (수치 포함)
- **AI 산출물 vs 사용자 개입**: AI가 어디까지 했고 사람이 어디를 고쳤는지
- **담당**: 개발 / 디자인
```

추가 규칙:
- 실패한 시도, 되돌린 결정, 오진단은 반드시 기록한다.
  이 프로젝트의 제출 문서에서 가장 설득력 있는 부분이 된다.
- WebGL 제약(용량, 금지 API, 저사양 브라우저) 때문에 내린 결정은
  그 제약을 명시적으로 적는다.
- 성능을 고려한 구현(sqrMagnitude 사용, 풀링 여부 등)은 그 이유를 남긴다.
- 외부 에셋·오픈소스·폰트를 추가하면 같은 커밋에서
  docs/AI_LOG.md 하단의 출처 표에 라이선스와 함께 행을 추가한다.
- 항목당 8줄을 넘기지 않는다.
