# TimeCore — 프로젝트 규칙

NHN 게임 AI 해커톤 예선 출품작. 2인 팀(개발 1, 디자인 1). 마감 임박.

## 기술 제약 (위반 시 빌드가 깨짐)

- 타깃은 **Unity 6 / Web(WebGL)** 빌드다. WebGL이 지원하지 않는 API를 쓰지 말 것:
  - 멀티스레딩 (`Thread`, `Task.Run`, `Parallel`)
  - `System.IO` 파일 접근 → 저장은 `PlayerPrefs`만
  - 소켓, `System.Net` 기반 통신
- 빌드 용량 = 로딩 시간이다. 새 패키지나 에셋 도입 전에 반드시 먼저 물어볼 것.
- 목표 사양은 저사양 노트북 브라우저다. 최적화보다 안정적 프레임이 우선.
- URP 2D Light는 적이 많아지면 비싸다. 분위기는 스프라이트 색조로 처리.

## Unity 컴파일 처리

- Auto Refresh는 켜져 있다. Ctrl+R 요청은 효과가 없으니 하지 마라.
- DLL 타임스탬프 폴링 절대 금지.
- 컴파일 결과는 Unity_GetConsoleLogs로만 확인한다.
- 스크립트가 인식되지 않으면 사용자에게 "Unity 종료 → Library/ScriptAssemblies 삭제 → 재시작"을 요청한다.

## 작업 방식

- 확장성보다 지금 돌아가는 것이 우선. 완벽한 아키텍처를 제안하지 말 것.
- 먼저 요청하지 않는 한 리팩터링하지 말 것.
- 새 도구나 워크플로우를 도입하지 말 것.
- 코드는 클래스 단위로 완결되게, 어느 GameObject에 붙이는지 명시할 것.

## 필수: 작업 세션 종료 시 로그 기록

의미 있는 작업(코드 생성/수정, 버그 해결, 설계 결정)을 끝낼 때마다
`docs/AI_LOG.md` 파일 **맨 아래에 append** 할 것. 기존 내용을 절대 수정/삭제하지 말 것.

형식:

```
### YYYY-MM-DD | 한 줄 요약

- **도구**: Claude Code (Opus 5) / Unity MCP / 기타
- **작업**: 무엇을 만들었거나 고쳤는지 1~3줄
- **프롬프트 원문**: 결정적이었던 지시를 그대로 인용 (요약 금지, 없으면 "해당 없음")
- **AI 산출물 vs 수정**: AI가 낸 코드를 어디까지 그대로 썼고 어디를 손봤는지
- **담당**: 개발 / 디자인
```

규칙:
- **프롬프트는 반드시 원문 그대로.** 이 항목은 제출물이므로 요약하면 가치가 없다.
- 외부 에셋이나 오픈소스를 추가했다면 `docs/AI_LOG.md` 하단의
  **에셋 출처 표**에도 같은 커밋에서 행을 추가할 것. 라이선스 명시는 제출 의무다.
- 항목당 6줄을 넘기지 말 것. 길면 안 읽힌다.
- 잘 안 된 시도, 되돌린 결정도 기록할 것. 문서에서 가장 설득력 있는 부분이 된다.
